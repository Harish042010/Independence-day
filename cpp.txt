#include<iostream>
using namespace std;
int main(){
int a,b;
char choice;
cin>>a>>b>>choice;
switch(choice)
	{
		case 1 ... 9: cout<<"single digit";
		break;	
		case 10 ... 99 : cout"double digit"
		break;
		default: cout<<"invalid choice";  //converting a variable from one data type to another -- typecasting
	}
}

_____________________________________________________________________________________________________________________________________

#include <iostream>
using namespace std;

int main() {

  int year;
  cin >> year;

  if (year % 400 == 0) {
    cout << year << " is leap year.";
  }
 
  else if (year % 100 == 0) {
    cout << year << "not a leap year.";
  }

  else if (year % 4 == 0) {
    cout << year << " is leap year.";
  }
 
  else {
    cout << year << " is leap year.";
  }

  return 0;
}